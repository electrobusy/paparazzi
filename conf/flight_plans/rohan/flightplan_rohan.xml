<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">

<flight_plan alt="1.0" ground_alt="0" lat0="51.990634" lon0="4.376789" max_dist_from_home="8" name="Bebop gcnet simulation" security_height="0.3">
  <!-- header -->
  <header>
      #include "subsystems/datalink/datalink.h"
      #include "subsystems/electrical.h"
      #include "subsystems/radio_control.h"
      #include "subsystems/ahrs.h"
      #define NPS_GAZEBO_WORLD "cyberzoo.world"
      #include "autopilot.h"
      inline void setNav(void){
      autopilot_mode_auto2 = AP_MODE_NAV;
      autopilot_static_set_mode(AP_MODE_NAV);
      }
      inline void setModule(void){
      autopilot_mode_auto2 = AP_MODE_MODULE;
      autopilot_static_set_mode(AP_MODE_MODULE);
      }
  </header> 

  <!-- waypoints -->
  <waypoints>

    <waypoint lat="51.990631" lon="4.376796" name="HOME"/> 

    <!-- Waypoints for testing the gcnet -->
    <waypoint name="Origin" x="0" y="0" alt="1."/>

    <!-- For the waypoint tracking scenario -->
    <waypoint name="WP1" x="-3.3" y="2.5" alt="1."/>

    <!-- Important for the exceptions -->
    <waypoint name="CLIMB" x="1.9" y="1.0"/>
    <waypoint name="STDBY" x="1.9" y="1.0"/>
    <waypoint name="TD" x="0.8" y="-1.7"/>
    <waypoint name="GOAL" x="1.9" y="1.0"/>
    <waypoint name="TRAJECTORY" x="1.9" y="1.0"/>

    <!-- Define the cyberzoo barrier - "flyable" region - see "sectors" -->
    <waypoint lat="51.9905834" lon="4.3767710" name="_CZ1"/>
    <waypoint lat="51.9906465" lon="4.3767025" name="_CZ2"/>
    <waypoint lat="51.9906882" lon="4.376805" name="_CZ3"/>
    <waypoint lat="51.9906238" lon="4.3768729" name="_CZ4"/>

    <!-- Define the obstacle zone - "non-flyable" region see "sectors" -->
    <waypoint lat="51.9905860" lon="4.3767712" name="_OZ1"/>
    <waypoint lat="51.9906456" lon="4.3767088" name="_OZ2"/>
    <waypoint lat="51.9906830" lon="4.3768048" name="_OZ3"/>
    <waypoint lat="51.9906239" lon="4.3768684" name="_OZ4"/>
  </waypoints>

  <!-- sectors -->
  <sectors>
    <sector color="red" name="CyberZoo">
      <corner name="_CZ1"/>
      <corner name="_CZ2"/>
      <corner name="_CZ3"/>
      <corner name="_CZ4"/>
    </sector>
    <sector color="#FF9922" name="ObstacleZone">
      <corner name="_OZ1"/>
      <corner name="_OZ2"/>
      <corner name="_OZ3"/>
      <corner name="_OZ4"/>
    </sector>
  </sectors>

  <!-- In case anything goes wrong, define exceptions, so then the drone can be recovered -->
  <exceptions>
    <!-- RC lost -->
    <!--exception cond="((radio_control.status == RC_REALLY_LOST) &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="Standby"/-->
    <!-- Datalink lost (constant RPM descent) -->
    <exception cond="((datalink_time > 5) &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Geofencing XY -->
    <!-- <exception cond="(!InsideCyberZoo(GetPosX(), GetPosY()) &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="land here"/> -->
    <!-- Geofencing Z 3.5 -->
    <exception cond="((GetPosAlt() > 3.5) &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Geofencing Z 4.5 (constant RPM descent)-->
    <exception cond="((GetPosAlt() > 4.5) &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      (autopilot_in_flight() == true) )" deroute="Landed"/>
    <!-- Bat low -->
    <exception cond="(electrical.bat_low &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Bat critical (constant RPM no stabilization)-->
    <exception cond="(electrical.bat_critical &&
      !(IndexOfBlock('Holding point') > nav_block) &&
      !(nav_block >= IndexOfBlock('land here')) &&
      (autopilot_in_flight() == true) )" deroute="land here"/>
  </exceptions>

  <!-- Important functions -->
  <blocks>
    <block name="Wait GPS">
        <call_once fun="NavKillThrottle()"/>
        <while cond="!GpsFixValid()"/>
    </block>
    <block name="Geo init">
        <while cond="LessThan(NavBlockTime(), 10)"/>
        <call_once fun="NavSetGroundReferenceHere()"/>
        <call_once fun="NavSetAltitudeReferenceHere()"/>
    </block>
    <block name="Holding point">
        <call_once fun="NavKillThrottle()"/>
        <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>
    <block name="Start engine">
        <call_once fun="NavResurrect()"/>  <!-- may need an additional resurrect if it doesn't start -->
        <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>
    <block name="Takeoff" strip_button="Takeoff" strip_icon="takeoff.png">
        <exception cond="GetPosAlt() > 0.5" deroute="Standby"/>
        <stay climb="nav_climb_vspeed" vmode="climb" wp="HOME"/>
    </block>
    <block name="Standby" strip_button="Standby" strip_icon="home.png">
        <stay wp="STDBY"/>
    </block>
    <block key="g" name="START" strip_button="Go" strip_icon="lookfore.png">
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <stay wp="GOAL"/>
    </block>
    <block name="STOP">
      <call_once fun="NavSetWaypointHere(WP_STDBY)"/>
      <stay wp="STDBY"/>
    </block>
    <!-- === MY BLOCKS === -->
    <block name="GoToWP1">
      <go wp="WP1"/>
      <stay wp="WP1"/>
    </block>
    <!-- Align to ensure that after landing, we don't need to reposition it manually -->
    <block name="Align right">
        <call_once fun="nav_set_heading_deg(90)"/>
        <return/>
    </block>
    <block name="Set module">
        <call_once fun="setModule()"/>
        <stay wp="WP1"/> <!-- This will not work, because we are not in NAV mode -->
    </block>
    <block name="Set nav">
        <call_once fun="setNav()"/>
        <stay wp="WP1"/>
    </block>    
    <!-- === MY BLOCKS === -->
    <block name="land here" strip_button="land here" strip_icon="land-right.png">
        <call_once fun="setNav()"/>
        <call_once fun="NavSetWaypointHere(WP_TD)"/>
        <go wp="TD"/>
        <deroute block="Flare"/>
    </block>
    <block name="Land">
        <call_once fun="setNav()"/>
        <go wp="TD"/>
        <deroute block="Flare"/>
    </block>
    <block name="Flare">
        <exception cond="0.1 > GetPosAlt()" deroute="Landed"/>
        <call_once fun="NavStartDetectGround()"/>
        <stay climb="nav_descend_vspeed" vmode="climb" wp="TD"/>
    </block>
    <block name="Landed">
        <call_once fun="NavKillThrottle()"/>
        <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>
  </blocks>
</flight_plan>
